// const searchInput = document.getElementById("searchInput");
// const questions = document.querySelectorAll(".question");

// searchInput.addEventListener("keyup", () => {
//   const searchTxt = searchInput.value.toLowerCase();

//   questions.forEach((question) => {
//     const questionText = question.dataset.question.toLowerCase();
//     const answerDiv = question.querySelector(".answer");

//     if (questionText.includes(searchTxt) && searchTxt !== "") {
//       answerDiv.classList.add("visible"); // Show the answer
//     } else {
//       answerDiv.classList.remove("visible"); // Hide the answer
//     }
//   });
// });